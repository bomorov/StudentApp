@model IEnumerable<StudentApp.Models.Group>

@{
    ViewBag.Title = "Group_List";
}

<h2>Group_List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Facultat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Facultat.Name)
        </td>
        <td>
            @Html.ActionLink("Edit Group Subjects", "Edit_Group_Subjects", new { id=item.Id }) |
            @Html.ActionLink("Group Subjects", "Group_Subjects", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
